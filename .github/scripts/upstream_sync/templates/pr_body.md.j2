## Upstream Sync -- {{ sync_date }}

**{{ status }}**

{% if not is_ff %}
> [!WARNING]
> Upstream history may have been rewritten (non-fast-forward). Verify the commit list carefully before merging.

{% endif %}
| | |
|---|---|
| **Source** | [`Cloud-Foundations/Dominator`](https://github.com/Cloud-Foundations/Dominator) `master` |
| **Target** | `PureStorage/Dominator` `master` |
| **New commits** | {{ commit_count }} |
| **Date range**  | {{ oldest_date }} to {{ newest_date }} |
| **Changes**     | {{ diffstat }} |

---

### Commits from Upstream

| SHA | Message | Author | Date |
|-----|---------|--------|------|
{{ commit_table }}

---

{% if has_conflicts %}
### Conflicting Files ({{ conflict_count }})

These files have merge conflicts and must be resolved manually before this PR can be merged:

```
{{ conflict_files }}
```

<details>
<summary>How to resolve locally</summary>

```bash
git fetch origin
git checkout master
git merge origin/{{ sync_branch }}
# Resolve each conflicting file, then:
git add <resolved-file> ...
git commit -m "chore: resolve upstream sync conflicts"
git push origin master
```

Once all conflicts are resolved and pushed to `master`, close this draft PR.
</details>

---

{% endif %}
> _Auto-generated by the [upstream-sync workflow](.github/workflows/upstream-sync.yml)_
> _Triggered by: `{{ event_name }}` | [Run #{{ run_number }}]({{ run_url }})_
