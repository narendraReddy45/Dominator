## Upstream Sync -- {{ sync_date }}

**{{ status }}**

{% if not is_ff %}
> [!WARNING]
> Fork's master has diverged from upstream. This PR will create a merge commit rather than a fast-forward. Verify the commit list carefully before merging.

{% endif %}
| | |
|---|---|
| **Source** | [`{{ upstream_repo }}`]({{ upstream_repo_url }}) `master` |
| **Target** | `{{ target_repo }}` `master` |
| **New commits** | {{ commit_count }} |
| **Date range**  | {{ oldest_date }} to {{ newest_date }} |
| **Changes**     | {{ diffstat }} |

---

### Commits from Upstream

| SHA | Message | Author | Date |
|-----|---------|--------|------|
{{ commit_table }}

---

{% if has_conflicts %}
### Conflicting Files ({{ conflict_count }})

These files have merge conflicts and must be resolved manually before this PR can be merged:

```
{{ conflict_files }}
```

<details>
<summary>How to resolve locally</summary>

```bash
git fetch origin
git checkout -b resolve-upstream-conflicts origin/master
git merge origin/{{ sync_branch }}
# Conflict markers will appear in the files listed above.
# Resolve each file, then:
git add <resolved-file> ...
git commit -m "chore: resolve upstream sync conflicts"
git push origin resolve-upstream-conflicts
```

Open a PR from `resolve-upstream-conflicts` into `master` and merge that instead.
Close this draft PR once the resolution PR is merged.
</details>

---

{% endif %}
> _Auto-generated by the [upstream-sync workflow](.github/workflows/upstream-sync.yml)_
> _Triggered by: `{{ event_name }}` | [Run #{{ run_number }}]({{ run_url }})_
